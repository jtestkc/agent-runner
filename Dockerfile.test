FROM python:3.13-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY scripts ./scripts
COPY tests ./tests

RUN pip install --upgrade pip && pip install -e ".[dev]"

CMD ["python", "-m", "pytest", "tests/test_core.py", "-v", "-k", "fc_"]
